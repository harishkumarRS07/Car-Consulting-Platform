services:
  - type: web
    runtime: static
    name: car-consulting-frontend
    rootDir: .
    buildCommand: cd frontend && npm install && npm run build && cd .. && cp -r frontend/dist ./dist
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
